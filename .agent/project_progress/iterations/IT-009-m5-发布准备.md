# IT-009 — M5 发布准备：LICENSE / CHANGELOG / 打包 / 发版

日期：2026-08-22
宏观：M5 发布准备（README 已先行交付，本迭代补齐其余发布件）

## 背景

M1–M4 完成：37 个公开方法、双语 README、Docker 本地服务、全门禁
（120 passed / 5 skipped）。`pyproject.toml` 声明 `license = "MIT"` 且
version = 0.1.0，但仓库**没有 LICENSE 文件**、没有 CHANGELOG，从未
打包/发版。`uv build` 此前因 PyPI TLS 间歇失败被记为阻塞，2026-08-22
复测已通（wheel + sdist 一次产出）。

## 目标

1. 发布件齐备：LICENSE、CHANGELOG.md、版本号（0.1.0 首发）、打包验证；
2. 按用户拍板的渠道完成首次发布（或产出可发布工件 + 发布清单）；
3. 发版记录写入 ROADMAP，打 git tag。

## 任务

- [x] 打包验证：`uv build` 产出 wheel + sdist（TLS 阻塞已消失）
- [x] LICENSE：补 MIT 许可证文件（author 与 pyproject 一致：comet）
- [x] CHANGELOG.md：按 Keep a Changelog 风格建文件，0.1.0 条目从
      ROADMAP 关键记录提炼（功能面：双客户端/37 方法/SSE 重连/重试/分层异常）
- [x] sdist 收紧：pyproject 加 `[tool.hatch.build.targets.sdist]` include/exclude，
      不再把 `.agent`/`.opencode`/`.claude`/`temp` 打进发布件
      （wheel METADATA 正确带 `License-Expression: MIT` + `License-File`）
- [ ] 发布渠道拍板：PyPI / 私有源 / 仅本地 dist（ROADMAP 待决事项）
- [ ] 按渠道执行发布（PyPI 需 `uv publish` + token；私有源同理）
- [ ] git tag `v0.1.0` + push tag
- [ ] ROADMAP 发版记录 + BOARD 里程碑 M5 ✅

## 完成记录

- 2026-08-22：LICENSE + CHANGELOG + sdist 收紧落地，`uv build` 干净产出
  wheel + sdist（sdist 顶层仅 src/examples/tests + 4 个发布文档 + PKG-INFO，
  无内部共享目录泄漏）。`make check` 绿（120 passed / 5 skipped）。
  **待用户拍板发布渠道**后执行发布 + tag。
