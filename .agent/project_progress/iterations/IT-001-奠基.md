# IT-001 项目奠基

- 状态：✅ 完成（2026-08-21）
- 所属里程碑：M1 奠基

## 目标

搭好项目骨架和质量工具链，让后续开发"改完即验证"。

## 任务

- [x] pyproject.toml：hatchling、Python >= 3.11、httpx + pydantic 依赖、dev 依赖组
- [x] src 布局：`src/opencode_client/`，`__init__.py` 导出，`py.typed`
- [x] 质量门禁配置：ruff（I/B/F/UP，line 120）、pyright strict、mypy 严格模式、pytest（asyncio auto）
- [x] `temp/`、`learning_log/` 排除出 lint/typecheck/收集范围
- [x] starter `client.py` 修复：pydantic v2（`ConfigDict`、`_tag`→alias）、`X | None` 现代化、
      `__aexit__` 完整签名、BasicAuth 类型
- [x] AGENTS.md：项目介绍、结构、常用命令、编码约定（含 wire 格式约定）
- [x] 全链路验证：ruff / format / mypy / pyright / pytest 全绿，包可安装可导入

## 完成记录

- 验证命令：`uv run ruff check . && uv run ruff format --check . && uv run mypy src/ tests/ && uv run pyright && uv run pytest`
- 遗留问题：无
