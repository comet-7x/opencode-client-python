# 本地 opencode serve（docker compose）的配置模板。
#
# 用法：cp .env.template .env，然后按需修改。.env 已被 .gitignore 排除；
# docker compose 会自动读取项目根目录的 .env，Makefile 的 docker-* 目标
# 也经它透传给 compose（OC_IMAGE/OC_PORT/OC_HOST 见 Makefile 顶部）。
#
# 任一变量留空即回落到默认值（compose 文件里的 ${VAR:-默认} 兜底）：
# 镜像 ghcr.io/anomalyco/opencode:1.18.21 / 端口 20001 / 绑定 0.0.0.0。

# 官方镜像（含 tag）。拉取慢时可改成域名代理，拉完建议 docker tag 还原官方名：
#   ghcr.nju.edu.cn/... 或 ghcr.m.daocloud.io/...（详见 .agent/AGENTS.md「本地服务（Docker）」）
OC_IMAGE=ghcr.io/anomalyco/opencode:1.18.21

# serve 监听的宿主机端口（容器内同端口，-p 两端一致）；
# 宿主机已有其他 opencode serve 占用时换端口（如 20002）
OC_PORT=20001

# 容器内监听地址。0.0.0.0 表示接受容器外（经端口映射的宿主机）连接，
# 不要改 127.0.0.1，否则宿主机映射不进来
OC_HOST=0.0.0.0
